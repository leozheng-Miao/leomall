server:
  port: 18810

spring:
  application:
    name: leomall-product-service
#
#  profiles:
#    active: dev

  # 数据源配置
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mall_product_system?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: clx740329
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000

  # Redis配置
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 10s

  # Elasticsearch配置
#  elasticsearch:
#    uris: ${ES_URIS:http://localhost:9200}
#    connection-timeout: 10s
#    socket-timeout: 30s

  # Cloud配置
#  cloud:
#    nacos:
#      discovery:
#        server-addr: localhost:17748
#        namespace: mall-dev
#      config:
#        server-addr: localhost:17748
#        namespace: mall-dev
#        file-extension: yml

# MyBatis Plus配置
mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.mall.product.entity
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: ASSIGN_ID
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

# MinIO配置
minio:
  endpoint: http://localhost:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: mall-product

# 缓存配置
cache:
  category:
    expire-time: 86400  # 分类缓存24小时
  product:
    expire-time: 3600   # 商品缓存1小时

#elasticsearch:
#  host: ${ES_HOST:localhost}
#  port: ${ES_PORT:9200}
#  scheme: ${ES_SCHEME:http}
#  username: ${ES_USERNAME:}
#  password: ${ES_PASSWORD:}



# Swagger配置
#springdoc:
#  api-docs:
#    enabled: true
#    path: /v3/api-docs
#  swagger-ui:
#    enabled: true
#    path: /swagger-ui.html
#  packages-to-scan: com.mall.product.controller

# 日志配置
#logging:
#  level:
#    com.mall.product: debug
#  pattern:
#    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"